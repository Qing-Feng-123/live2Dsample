# 桃濑日和 (Hiyori Momose) - FREE 动作素材完整文档

> **模型版本**: Cubism 3.0 SDK 标准模型 | **公开日期**: 2021年6月10日  
> **插画**: Kani Biimu | **模型制作**: Live2D  
> **许可证**: 小规模企业/个人可商用 (需遵守[Live2D示例模型使用授权协议](https://www.live2d.com/zh-CHS/download/sample-data/))

---

## 一、可操纵参数 (29个)

### 1. 脸部 (ParamGroupFace) — 4个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamAngleX` | 角度 X | 面部水平旋转角度 |
| `ParamAngleY` | 角度 Y | 面部垂直旋转角度 |
| `ParamAngleZ` | 角度 Z | 面部倾斜旋转角度 |
| `ParamCheek` | 脸颊泛红 | 脸颊红晕程度 |

### 2. 眼睛 (ParamGroupEyes) — 4个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamEyeLOpen` | 左眼 开闭 | 左眼睁开/闭合 (0=闭眼, 1=睁眼) |
| `ParamEyeLSmile` | 左眼 微笑 | 左眼笑眼弧度 |
| `ParamEyeROpen` | 右眼 开闭 | 右眼睁开/闭合 (0=闭眼, 1=睁眼) |
| `ParamEyeRSmile` | 右眼 微笑 | 右眼笑眼弧度 |

### 3. 眼珠 (ParamGroupEyeballs) — 2个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamEyeBallX` | 眼珠 X | 眼珠水平移动 |
| `ParamEyeBallY` | 眼珠 Y | 眼珠垂直移动 |

### 4. 眉毛 (ParamGroupBrows) — 2个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamBrowLForm` | 左眉 变形 | 左眉形状/角度 |
| `ParamBrowRForm` | 右眉 变形 | 右眉形状/角度 |

### 5. 嘴 (ParamGroupMouth) — 2个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamMouthForm` | 嘴 变形 | 嘴型/微笑弧度 |
| `ParamMouthOpenY` | 嘴 开闭 | 嘴巴张开程度 (唇形同步) |

### 6. 身体 (ParamGroupBody) — 4个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamBodyAngleX` | 身体的旋转 X | 身体水平旋转 |
| `ParamBodyAngleY` | 身体的旋转 Y | 身体垂直旋转 |
| `ParamBodyAngleZ` | 身体的旋转 Z | 身体倾斜旋转 |
| `ParamBreath` | 呼吸 | 呼吸幅度 (0~1正弦波) |

### 7. 手臂 (ParamGroupArms) — 2个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamArmLA` | 左臂 A | 左臂角度/位置 |
| `ParamArmRA` | 右臂 A | 右臂角度/位置 |

### 8. 摇动/物理 (ParamGroupSway) — 9个参数
| 参数ID | 中文名 | 说明 |
|---|---|---|
| `ParamBustY` | 胸部 摇动 | 胸部物理摆动 |
| `ParamHairAhoge` | 头发摇动 呆毛 | 呆毛物理摆动 |
| `ParamHairFront` | 头发摇动 前 | 前发物理摆动 |
| `ParamHairSide` | 头发摇动 侧 | 侧发物理摆动 |
| `ParamHairBack` | 头发摇动 后 | 后发物理摆动 |
| `ParamHairSideUp` | 辫子的摇动 | 侧边辫子摆动 |
| `ParamRibbon` | 蝴蝶结的摇动 | 身体蝴蝶结摆动 |
| `ParamSkirt` | 短裙的摇动 | 短裙物理摆动 |
| `ParamSideUpRibbon` | 发饰的摇动 | 头部发饰蝴蝶结摆动 |

---

## 二、可绘制部件 (17个)

| 部件ID | 中文名 | 说明 |
|---|---|---|
| `PartCore` | Core | 核心部件(始终绘制) |
| `PartCheek` | 脸颊 | 脸颊红晕 |
| `PartBrow` | 眉毛 | 左右眉毛 |
| `PartEye` | 眼睛 | 眼睛本体 |
| `PartEyeBall` | 眼珠 | 眼球 |
| `PartNose` | 鼻子 | 鼻子 |
| `PartMouth` | 口 | 嘴巴 |
| `PartFace` | 脸 | 面部轮廓 |
| `PartEar` | 耳朵 | 耳朵 |
| `PartHairSide` | 侧发 | 侧面头发 |
| `PartHairFront` | 前发 | 前额头发(含呆毛) |
| `PartHairBack` | 后发 | 后脑头发 |
| `PartNeck` | 脖子 | 脖子 |
| `PartBody` | 身体 | 身体躯干 |
| `PartArmA` | 手臂A | 双臂(通过PartOpacity控制显隐) |
| `PartBackground` | 背景 | 背景层 |
| `PartSketch` | [参考图] | 参考图(调试用) |

---

## 三、物理模拟 (7组)

### 物理全局参数
| 参数 | 值 |
|---|---|
| 重力 X | 0 |
| 重力 Y | -1 |
| 风力 X | 0 |
| 风力 Y | 0 |
| 物理设定总数 | 7 |
| 输入总数 | 24 |
| 输出总数 | 7 |
| 顶点总数 | 14 |

### 物理设定详情

#### PhysicsSetting1 — 前髪 (前发)
| 属性 | 详情 |
|---|---|
| **输入源** | `ParamAngleX`(权重60, X方向), `ParamAngleZ`(权重60, 角度), `ParamBodyAngleX`(权重40, X方向), `ParamBodyAngleZ`(权重40, 角度) |
| **输出目标** | `ParamHairFront` (缩放1.5, 权重100, 角度型) |
| **顶点** | V0: 锚点(0,0), Mobility=1; V1: 自由端(0,3), Mobility=0.95, Delay=0.9, Acceleration=1.5, Radius=3 |
| **归一化** | 位置: -10~10, 角度: -10~10 |

#### PhysicsSetting2 — 横髪 (侧发)
| 属性 | 详情 |
|---|---|
| **输入源** | `ParamAngleX`(60), `ParamAngleZ`(60), `ParamBodyAngleX`(40), `ParamBodyAngleZ`(40) |
| **输出目标** | `ParamHairSide` (缩放3, 权重100, 角度型) |
| **顶点** | V0: 锚点(0,0); V1: 自由端(0,15), Mobility=0.95, Delay=0.8, Acceleration=1.5, Radius=15 |
| **归一化** | 位置: -10~10, 角度: -10~10 |

#### PhysicsSetting3 — 後ろ髪 (后发)
| 属性 | 详情 |
|---|---|
| **输入源** | `ParamAngleX`(60), `ParamAngleZ`(60), `ParamBodyAngleX`(40), `ParamBodyAngleZ`(40) |
| **输出目标** | `ParamHairBack` (缩放2.132, 权重100, 角度型) |
| **顶点** | V0: 锚点(0,0); V1: 自由端(0,15), Mobility=0.95, Delay=0.8, Acceleration=1.5, Radius=15 |
| **归一化** | 位置: -10~10, 角度: -10~10 |

#### PhysicsSetting4 — サイドアップ (辫子)
| 属性 | 详情 |
|---|---|
| **输入源** | `ParamAngleX`(60), `ParamAngleZ`(60), `ParamBodyAngleX`(40), `ParamBodyAngleZ`(40) |
| **输出目标** | `ParamHairSideUp` (缩放2.161, 权重100, 角度型) |
| **顶点** | V0: 锚点(0,0); V1: 自由端(0,15), Mobility=0.95, Delay=0.8, Acceleration=1.5, Radius=15 |
| **归一化** | 位置: -10~10, 角度: -10~10 |

#### PhysicsSetting5 — スカート (短裙)
| 属性 | 详情 |
|---|---|
| **输入源** | `ParamBodyAngleX`(100), `ParamBodyAngleZ`(100) |
| **输出目标** | `ParamSkirt` (缩放1, 权重100, 角度型) |
| **顶点** | V0: 锚点(0,0); V1: 自由端(0,10), Mobility=0.9, Delay=0.6, Acceleration=1.5, Radius=10 |
| **归一化** | 位置: -10~10, 角度: -10~10 |

#### PhysicsSetting6 — リボン 体 (身体蝴蝶结)
| 属性 | 详情 |
|---|---|
| **输入源** | `ParamBodyAngleX`(100), `ParamBodyAngleZ`(100) |
| **输出目标** | `ParamRibbon` (缩放1.559, 权重100, 角度型) |
| **顶点** | V0: 锚点(0,0); V1: 自由端(0,10), Mobility=0.9, Delay=0.6, Acceleration=1.5, Radius=10 |
| **归一化** | 位置: -10~10, 角度: -10~10 |

#### PhysicsSetting7 — リボン 頭 (头部发饰)
| 属性 | 详情 |
|---|---|
| **输入源** | `ParamAngleX`(60), `ParamAngleZ`(60), `ParamBodyAngleX`(40), `ParamBodyAngleZ`(40) |
| **输出目标** | `ParamSideUpRibbon` (缩放1.441, 权重100, 角度型) |
| **顶点** | V0: 锚点(0,0); V1: 自由端(0,10), Mobility=0.9, Delay=0.6, Acceleration=1.5, Radius=10 |
| **归一化** | 位置: -10~10, 角度: -10~10 |

---

## 四、特殊功能组

### 4.1 自动眨眼组 (EyeBlink)
| 目标 | 参数 |
|---|---|
| `Parameter` | `ParamEyeLOpen`, `ParamEyeROpen` |

> 双眼同步眨眼，用于自动眨眼控制器。

### 4.2 唇形同步组 (LipSync)
| 目标 | 参数 |
|---|---|
| `Parameter` | `ParamMouthOpenY` |

> 用于音频驱动的口型同步。

### 4.3 胶水功能 (Glue)
> 该模型的肩部应用了 Live2D Cubism 3.0 新功能「胶水」(Glue)，用于在变形时保持网格连续性，防止关节处撕裂。

### 4.4 点击区域
| ID | 名称 |
|---|---|
| `HitArea` | Body |

> 点击/触摸检测区域为身体(Body)整体。

---

## 五、组合联动 (CombinedParameters)

模型定义了3组双向联动参数对，在编辑器中调整时可联动操作：

| 联动组 | 参数1 | 参数2 | 说明 |
|---|---|---|---|
| 头部角度联动 | `ParamAngleX` | `ParamAngleY` | 面部X/Y旋转联动调整 |
| 眼球方向联动 | `ParamEyeBallX` | `ParamEyeBallY` | 眼珠XY方向联动 |
| 嘴部形态联动 | `ParamMouthForm` | `ParamMouthOpenY` | 嘴型与张开度联动 |

---

## 六、动作文件列表

| 文件名 | 动作组 | 时长 | 帧率 | 循环 | 曲线数 | 说明 |
|---|---|---|---|---|---|---|
| `hiyori_m01.motion3.json` | Idle | 4.70s | 30fps | 是 | 22 | 待机动画1 (头部摆动+眨眼) |
| `hiyori_m02.motion3.json` | Idle | 5.93s | 30fps | 是 | 22 | 待机动画2 (呼吸+微笑+身体摆动) |
| `hiyori_m05.motion3.json` | Idle | 8.57s | 30fps | 是 | 22 | 待机动画3 (长循环综合动作) |
| `hiyori_m03.motion3.json` | Flick | 4.20s | 30fps | 是 | 22 | 滑动/轻弹响应 |
| `hiyori_m04.motion3.json` | FlickDown | 4.37s | 30fps | 是 | 22 | 向下滑动响应 |
| `hiyori_m06.motion3.json` | Tap | 1.90s | 30fps | 是 | 22 | 点击头部响应 |
| `hiyori_m07.motion3.json` | Tap@Body | 1.60s | 30fps | 是 | 22 | 点击身体响应 |
| `hiyori_m08.motion3.json` | Flick@Body | 4.17s | 30fps | 是 | 22 | 滑动身体响应 |

### 动作组映射
| 动作组 | 触发方式 | 包含动作 |
|---|---|---|
| `Idle` | 默认待机 | m01, m02, m05 |
| `Flick` | 滑动头部 | m03 |
| `FlickDown` | 向下滑动头部 | m04 |
| `Tap` | 点击头部 | m06 |
| `Tap@Body` | 点击身体 | m07 |
| `Flick@Body` | 滑动身体 | m08 |

---

## 七、文件结构

```
workspace/
├── hiyori_free_t08.cmo3              # 组合模型文件(含物理)
├── hiyori_free_t03.can3              # 基本动画数据
├── ReadMe.txt                         # 说明文档
└── runtime/
    ├── hiyori_free_t08.moc3           # 模型核心数据
    ├── hiyori_free_t08.model3.json    # 模型设定(文件引用/Groups/HitAreas)
    ├── hiyori_free_t08.physics3.json  # 物理模拟设定
    ├── hiyori_free_t08.cdi3.json      # 参数/部件/组合联动定义
    ├── hiyori_free_t08_old.cdi3.json  # 旧版参数定义(备份)
    ├── hiyori_free_t08.2048/
    │   └── texture_00.png             # 2048分辨率贴图
    └── motion/
        ├── hiyori_m01.motion3.json    # Idle待机1
        ├── hiyori_m02.motion3.json    # Idle待机2
        ├── hiyori_m03.motion3.json    # Flick滑动
        ├── hiyori_m04.motion3.json    # FlickDown向下滑动
        ├── hiyori_m05.motion3.json    # Idle待机3
        ├── hiyori_m06.motion3.json    # Tap点击
        ├── hiyori_m07.motion3.json    # Tap@Body身体点击
        └── hiyori_m08.motion3.json    # Flick@Body身体滑动
```

---

## 八、快速接入指南

### SDK 加载模型 (JavaScript/TypeScript)

```javascript
// 加载模型
const modelHomeDir = "runtime/";
const modelSetting = new Live2DModelSettingJson(
  "runtime/hiyori_free_t08.model3.json"
);

// 关键参数控制示例
const model = live2DModel;

// 面部角度
model.setParameterValueById("ParamAngleX", value);   // -30 ~ 30
model.setParameterValueById("ParamAngleY", value);   // -30 ~ 30
model.setParameterValueById("ParamAngleZ", value);   // -30 ~ 30

// 眨眼
model.setParameterValueById("ParamEyeLOpen", value); // 0 ~ 1
model.setParameterValueById("ParamEyeROpen", value); // 0 ~ 1

// 唇形同步
model.setParameterValueById("ParamMouthOpenY", value); // 0 ~ 1

// 身体呼吸
model.setParameterValueById("ParamBreath", value);    // 0 ~ 1

// 手臂
model.setParameterValueById("ParamArmLA", value);     // -10 ~ 10
model.setParameterValueById("ParamArmRA", value);     // -10 ~ 10

// 部件显隐
model.setPartOpacityById("PartArmA", opacity);        // 0 ~ 1
model.setPartOpacityById("PartHairFront", opacity);   // 0 ~ 1

// 播放动作
model.startMotion("Idle", 0, Live2D.MotionPriority.FORCE);
model.startMotion("Tap", 0, Live2D.MotionPriority.FORCE);
```